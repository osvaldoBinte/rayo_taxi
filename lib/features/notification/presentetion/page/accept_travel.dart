import 'package:flutter/material.dart';

class AcceptTravelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Aceptar Viaje'),
      ),
      body: Center(
        child: Text('Aquí puedes aceptar el viaje.'),
      ),
    );
  }
}
